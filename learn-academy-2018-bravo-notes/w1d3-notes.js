/*

---------------------------JavaScript Arrays----------------------------
"Declare" an array
"Elements" are the values that make up an array
"Index" is the location of an element in an array


-An array is a special variable that can hold multiple values
-You can put variables or expressions in your arrays or even other Arrays
-Index always starts at 0.  Ex. [0,1,2,3,4,5]

---------------------------Array Methods----------------------------

-Just like numbers and strings, arrays have methods. You can use the ".pop" method
to remove an element from and array and returns that element. The ".reverse"
method is used to reverse the order of the elements in an array

Ex.
["apple", "orange", "kiwi"].pop();
"kiwi"
[1, 2, 3].reverse();
[3, 2, 1]

---------------------------Adding New Elements----------------------------

-You can add elements to an array by using the ".push" method or combine elements
by using the ".concat" method

Ex.
var greetings = [];
greetings.push('hi');
greetings.push('hello');
greetings.concat(['hola', 'buenos dias']);

---------------------------Accessing Elements----------------------------

-You can access elements in an array by calling the array and the index location
 of the element   Ex. array[1]

 ---------------------------Assign/Change Element Values----------------------------

-You can Assign/change the value of an element in an array by by calling the array and
 the index location of the element then assigning it to a mnew value closed by
 a semicolon   Ex. array[1] = 10;

 --------------------------- Assigning Element Values----------------------------
